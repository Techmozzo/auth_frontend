import React, { useState } from 'react';

const ViewConculsion = ({ conclusion }) => {
  const [state, setState] = useState();

  return (
    <div>
      Enter
    </div>
  );
};

export default ViewConculsion;
